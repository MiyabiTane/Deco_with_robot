doctype html
html(lang='en')
  head
    title test
    script(src="https://cdn.jsdelivr.net/npm/gl-matrix@3/gl-matrix.js")
    script. 
      var degree = #{degree};
      var reload = #{reload};
      if (reload === 1) {
        setTimeout("location.reload()", 1000*3);
      }
  body
    <canvas id="webgl"></canvas>
    script(type="text/javascript", src="/javascripts/rbrow.js")
    // meta(http-equiv="refresh", content="3")
    script.
      var reload = #{reload};
      if (reload == 2) {
        var form = document.createElement('form');
        form.action = '/';
        form.method = 'POST';
    
        document.body.append(form);

        form.addEventListener('formdata', (e) => {
          var fd = e.formData;
      
          fd.set('degree', '-20');
        });

        form.submit();
      }

